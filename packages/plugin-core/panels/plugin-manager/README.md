# Plugin Manager Panel Source

这个目录是 builtin plugin-manager panel 静态资源的源码主目录。

当前约定：

- 修改 `embedded/index.html` / `embedded/index.css` / `embedded/index.js` 时，优先改这里
- `peanut-plugin-panel/panels/plugin-manager` 只保留壳层镜像
